export class Time {
    sleep(ms) {
        let start = get_time();;
        let end = start + ms;

        let currtime = get_time();;
        while (currtime != end) {
            currtime = get_time();;
        }
    }
    
    get() {
        return get_time();
    }
}