--------------------------------------------------------------------------------

                    How do I want to handle modules and imports?

    - Local modules are imported with a relative path.
    - Installed Modules (from package manager) are imported with a package name.

    - Modules Contain public and private functions. (public functions are exported with the keyword export)
    - Example: export function publicFunction() { ... }
    - Example: function privateFunction() { ... }

    (IDEA)
    - Can import a function from a module with the keyword include.
    - Example: include { publicFunction } = './module.dem'
    - Example: include { publicFunction } = 'module'
    
    - Imports are hoisted to the top of the file.
    - Imports are evaluated before the rest of the file.

    (IDEA)
    - Imports are cached. (If a module is imported twice, it will only be evaluated once.)

    (IDEA)
    - Imports can be aliased.
    - Example: include { publicFunction as alias } = './module.dem'

    (IDEA)
    Do we have GLOBALS & LOCALS (To be implemented if so)
    - Globals can be seen by all programs no matter what
    - Locals can be seen by 1 program by defining or importing
    - Local scope = program file
    - Global scope = all programs
    - Globals also import default imports (eg, stdio, stdlib, math, time)
    - Exports are added to an array of Demi variables which can be appended onto the programs locals after importing

--------------------------------------------------------------------------------
                Post-Objects Development Roadmap for Demi Lang

            1. Class System Implementation (High Priority)
                -(1) Basic class syntax and instantiation
                -(2) Constructor support
                -(3) Instance properties
                -(4) Method definitions
                -(5) Inheritance system
                -(6) Static methods and properties (maybe)

                Example:
                (1) class Person {
                        name: String
                        age: Number

                (2)     construct(name: String, age: Number) {
                (3)         this.name = name
                            this.age = age
                        }

                (4)     function greet() {
                            print("Hello, I am " + this.name)
                        }
                    }

                    // Example usage:
                    let person = Person("Alice", 25)
                    person.greet()


            2. Module System (Medium Priority)
                - Constructor supportBasic import/export syntax
                - Method definitionsModule caching mechanism
                - Instance propertiesSupport for relative paths
                - Inheritance systemPackage resolution system
                - Static methods and propertiesStandard library organization

                Notes:
                How do we want to parse this?
                - Module importing process:
                    1. When a module is imported, read and parse the file into AST
                    2. Create new scope for module execution
                    3. Execute module code in isolated scope
                    4. Cache module exports
                    5. Return only exported values to importing scope

                - Benefits:
                    - Modules run in isolated scope
                    - Single evaluation per module
                    - Proper encapsulation
                    - Circular dependency handling


            3. Standard Library Enhancement (Medium Priority)
                - Core Modules:
                    - stdio - I/O operations
                    - time - Time operations (expand current implementation)
                    - math - Mathematical operations
                    - fs - File system operations
                    
            4. Error Handling (High Priority)
                - Custom error classes
                - Try/catch mechanism
                - Stack trace implementation
                - Error propagation system

            5. Type System Enhancement (Low Priority)
                - Type checking improvements
                - Type inference
                - Interface definitions
                - Generic types

            6. Developer Experience (Ongoing)
                - Better error messages
                - Source maps
                - Debugging support
                - Documentation generation
                - Language server protocol support

            7. Testing Framework (Medium Priority)
                - Unit testing framework
                - Assertion library
                - Test runner
                - Mocking system
                - Suggested Implementation Order:
                - Classes (as it builds directly on objects)
                - Error Handling (needed for robust module system)
                - Module System (enables code organization)
                - Standard Library (leverages modules)
                - Testing Framework
                - Type System
                - Developer Experience (continuous improvement)

--------------------------------------------------------------------------------

1. In parser.ts, I see a method stub for object parsing:

private parse_object_expr(): Expr {
    // Implementation appears to be missing
}

2. In values.ts, there is an interface for objects:

export interface ObjectVal extends RuntimeVal {
    type: "object";
    properties: Map<string, RuntimeVal>;
    scope?: Environment;
    parentEnv?: Environment;
}

3. There's a helper function to create objects:

export function MK_OBJECT(properties: Map<string, RuntimeVal>): RuntimeVal {
    // Implementation exists but not shown in the snippets
}

Based on the code shown:
    1. The basic infrastructure for objects exists (interfaces and types)
    2. The parser has a placeholder for object expressions
    3. The runtime has object value types defined
    4. But I don't see any actual implementation of object handling in the parser or evaluator

This suggests you're at the planning stage for object implementation, with the basic type system in place,
but haven't yet implemented the actual object handling logic.

The next steps would likely be:
    1. Implement parse_object_expr() in the parser
    2. Add object property access handling
    3. Implement object evaluation in the runtime
    4. Add object mutation capabilities
                    (Refresher of what mutation is.)


--------------------------------------------------------------------------------
                    Summary Of What I've Done Today (03/26/2025) 

        1. Parser Adjustments :
            -   We focused on refining the parser logic in your TypeScript
                code to ensure that parentheses ( () ) are only treated as
                part of a function call when explicitly required (e.g., for
                arrow functions or fn declarations). Otherwise, they are parsed
                as parenthesized expressions.
            -   This involved modifying methods like parse_primary_expr and
                ensuring that parse_call_expr and parse_call_member_expr only
                handle valid function calls.

        2. Enhancing the run_examples.ps1 Script :
            -   We updated the PowerShell script to provide more detailed
                feedback after running all example .dem files.
            -   Added logic to capture the output of each script execution and
                detect errors dynamically.
            -   Introduced counters for total examples, successful runs, and failures.
            -   Logged detailed failure information, including the example name and
                error message, for easier debugging.
            -   Added a summary section at the end of the script to display the results
                in a clear and organized manner.
    
        3. Error Handling in run_examples.ps1 :
            -   Implemented logic to check the output of each executed command for the
                presence of errors (e.g., by matching the word "Error").
            -   Ensured that both runtime errors and command exceptions are properly
                captured and logged.

These changes aim to improve the robustness of the parser and the usability of your example execution workflow,
making it easier to debug and analyze issues. Let me know if you'd like to revisit or expand on any of these areas!