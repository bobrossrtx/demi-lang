const foo = 42;

const obj = {
    x: 100,
    y: 32,
    foo,
    complex: {
        bar: true
    }
};

print(obj);

print(obj.x);

print(obj.y);

# print(get_globals());

# print(obj["x"]);

# let f = obj.complex.bar;

# print(f);